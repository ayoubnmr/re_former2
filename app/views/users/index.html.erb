

<% @users.each do |user|%>
<%= user.username%><br>
<%= user.email%><br>
<%= user.password%><br>

  <li><%= link_to "Edit", edit_user_path(user) %></li>
  <li><%= link_to "delet", user_path(user) , data: {
                          turbo_method: :delete,
                          turbo_confirm: "Are you sure?" }    %></li>
<%end%>
